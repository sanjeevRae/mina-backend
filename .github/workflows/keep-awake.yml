name: Keep Render App Awake

on:
  schedule:
    - cron: '*/14 * * * *'
  workflow_dispatch:

jobs:
  ping-render:
    runs-on: ubuntu-latest
    steps:
      - name: Curl health check endpoint
        run: |
          curl -sSf https://mina-backend-1.onrender.com/health || exit 1
